# 冲击魔法 — 力度合成能力商店化 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把"两材料大力对撞就合成"做成商店分阶段购买的魔法能力，进正式 Tavern 旁路厨具；力度太狠出劣质成品，并把一直被无视的 `quality` 字段接进上菜收益。

**Architecture:** 纯逻辑下沉到 RefCounted 子系统（CraftSystem 管解锁状态+配方反查+力度分档，EconomySystem 管品质收益），可 headless 单测；GameManager 加 `buy_ability` 与存档字段并在上菜时消费 quality；`bar_workspace.gd` 把物理碰撞接到 CraftSystem 的纯逻辑上生成成品；商店 UI 在 `day_map_view.gd` 加"技法"区。子系统不互引，全走 GameManager 中介（项目硬规则）。

**Tech Stack:** Godot 4.6.x standard, GDScript, JSON 数据驱动, headless 场景测试（无 test runner）。

---

## 约定：本计划所有 headless 命令的 `<godot>`

项目无 test runner，用 Godot 控制台二进制跑 headless 场景（见 `docs/18` §4）：

```
<godot> = "C:/Program Files/Godot_v4.6.3-stable_mono_win64/Godot_v4.6.3-stable_mono_win64_console.exe"
```

判定标准：看 stdout 有无 `ALL PASS` 且无 `FAIL` / `SCRIPT ERROR` / `Parse Error` 关键字，**不要只看 exit code**。

每个新增测试都要 `git add` 测试 `.gd` + `.tscn`。提交信息用 `type: 简述`（feat/fix/refactor/docs），结尾加：
```
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
**禁止 `git add .` / `git add -A`，逐文件 stage。** 当前分支 `feat/seasoning-system`，本功能就在此分支做（用户要求）。

---

## 文件结构（先锁定边界）

| 文件 | 职责 | 动作 |
|---|---|---|
| `data/shop.json` | 新增 `abilities` 段 | 改 |
| `data/slam.json` | 力度窗口阈值（热调） | 建 |
| `scripts/systems/shop_system.gd` | 加载+查询 abilities 价格/名称 | 改 |
| `scripts/systems/craft_system.gd` | 砸解锁状态、`find_slam_recipe`、`classify_slam_force`、加载 slam.json | 改 |
| `scripts/systems/economy_system.gd` | `gold_for_quality` / `reputation_for_quality` 纯函数 | 改 |
| `scripts/game_manager.gd` | `buy_ability`、`is_ability_owned`、上菜消费 quality、存档字段、`notify_stage_caption` 转发 | 改 |
| `scripts/ui/bar_workspace.gd` | 物理碰撞 → 砸合成（接 CraftSystem 纯逻辑） | 改 |
| `scripts/ui/day_map_view.gd` | 商店"技法"区 UI | 改 |
| `scripts/test/test_impact_magic.gd` + `scenes/test/test_impact_magic.tscn` | 全部纯逻辑单测 | 建 |

`bar_workspace` 与 `day_map_view` 的物理/UI 部分靠编辑器走查（Task 8），其余全部 headless 单测覆盖。

---

## Task 1：ShopSystem 加载 abilities

**Files:**
- Modify: `data/shop.json`
- Modify: `scripts/systems/shop_system.gd`
- Test: `scripts/test/test_impact_magic.gd` + `scenes/test/test_impact_magic.tscn`（本任务新建测试骨架）

- [ ] **Step 1：建测试骨架并写第一组失败断言**

新建 `scripts/test/test_impact_magic.gd`：

```gdscript
extends Node

## 冲击魔法功能单元测：shop abilities / 砸解锁状态 / 配方反查 / 力度分档 /
## buy_ability / quality 收益 / 存档往返。物理碰撞与商店 UI 走编辑器走查。

var _checks := 0
var _failures := 0


func _ready() -> void:
	_test_shop_abilities()
	_finish()


func _ok(cond: bool, msg: String) -> void:
	_checks += 1
	if not cond:
		_failures += 1
		push_error("[TEST-IMPACT] FAIL: " + msg)


func _finish() -> void:
	if _failures == 0:
		print("[TEST-IMPACT] ALL PASS (", _checks, " checks)")
		get_tree().quit(0)
	else:
		push_error("[TEST-IMPACT] FAILURES: %d / %d checks" % [_failures, _checks])
		get_tree().quit(1)


func _gm():
	return get_node("/root/GameManager")


func _test_shop_abilities() -> void:
	var s = _gm().shop
	_ok(s.get_ability_price("slam_pot") == 60, "slam_pot 价格应为 60")
	_ok(s.get_ability_price("slam_barrel") == 100, "slam_barrel 价格应为 100")
	_ok(s.get_ability_name("slam_pot") == "冲击魔法·炖锅", "slam_pot 名称")
	_ok(s.get_ability_price("nope") == -1, "未知能力价格应为 -1")
	_ok(s.get_ability_keys().size() == 2, "应有 2 个能力")
```

新建 `scenes/test/test_impact_magic.tscn`：

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/test/test_impact_magic.gd" id="1"]

[node name="TestImpactMagic" type="Node"]
script = ExtResource("1")
```

- [ ] **Step 2：跑测试确认失败**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: 报错（`get_ability_price` 方法不存在 / SCRIPT ERROR），无 `ALL PASS`。

- [ ] **Step 3：改 `data/shop.json` 加 abilities 段**

在 `miraDiscount` 之前插入（注意保留逗号合法）：

```json
  "abilities": [
    { "key": "slam_pot", "name": "冲击魔法·炖锅", "price": 60 },
    { "key": "slam_barrel", "name": "冲击魔法·酒桶", "price": 100 }
  ],
```

最终结构：`materials`、`recipeUnlocks`、`abilities`、`miraDiscount` 四个顶层键。

- [ ] **Step 4：改 `scripts/systems/shop_system.gd` 加载与查询**

在 `var _recipe_unlock_prices: Dictionary = {}` 下新增字段：

```gdscript
var _ability_prices: Dictionary = {}
var _ability_names: Dictionary = {}
```

在 `load_config()` 里、`_recipe_unlock_prices` 填充之后、`print(...)` 之前插入：

```gdscript
	_ability_prices.clear()
	_ability_names.clear()
	if data.has("abilities") and data["abilities"] != null:
		for a in data["abilities"]:
			_ability_prices[a["key"]] = a["price"]
			_ability_names[a["key"]] = a["name"]
```

在文件末尾新增查询方法：

```gdscript
func get_ability_price(key: String) -> int:
	return _ability_prices.get(key, INVALID_RECIPE_PRICE)

func get_ability_name(key: String) -> String:
	return _ability_names.get(key, key)

func get_ability_keys() -> Array:
	return _ability_prices.keys()
```

- [ ] **Step 5：跑测试确认通过**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: `[TEST-IMPACT] ALL PASS (5 checks)`，无 FAIL/ERROR。

- [ ] **Step 6：提交**

```bash
git add data/shop.json scripts/systems/shop_system.gd scripts/test/test_impact_magic.gd scenes/test/test_impact_magic.tscn
git commit -m "feat: 商店加载冲击魔法能力定价 + 测试骨架"
```

---

## Task 2：CraftSystem 砸解锁状态 + 力度分档

**Files:**
- Modify: `scripts/systems/craft_system.gd`
- Create: `data/slam.json`
- Test: `scripts/test/test_impact_magic.gd`

- [ ] **Step 1：加失败断言**

在 `test_impact_magic.gd` 的 `_ready()` 里、`_finish()` 之前加 `_test_slam_state()`，并新增方法：

```gdscript
func _test_slam_state() -> void:
	var c = _gm().craft
	# 解锁状态从干净起步（测试间互不污染：先清）
	c.unlocked_slam_containers.clear()
	_ok(not c.is_slam_unlocked("pot"), "初始 pot 未解锁")
	c.unlock_slam("pot")
	_ok(c.is_slam_unlocked("pot"), "解锁后 pot 已解锁")
	c.unlock_slam("pot")  # 幂等
	_ok(c.unlocked_slam_containers.size() == 1, "重复解锁不应增加")
	# 力度分档
	_ok(c.classify_slam_force(100.0) == "none", "100 应不合成")
	_ok(c.classify_slam_force(500.0) == "normal", "窗口内应 normal")
	_ok(c.classify_slam_force(1500.0) == "poor", "超上限应 poor")
```

- [ ] **Step 2：跑测试确认失败**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: FAIL/ERROR（`unlocked_slam_containers` 不存在）。

- [ ] **Step 3：建 `data/slam.json`**

```json
{
  "merge_low": 350.0,
  "merge_high": 1000.0
}
```

- [ ] **Step 4：改 `scripts/systems/craft_system.gd`**

在 `var unlocked_recipes: Array = []` 下新增字段：

```gdscript
var unlocked_slam_containers: Array = []   # 已购冲击魔法的容器类，如 ["pot","barrel"]
var slam_merge_low: float = 350.0
var slam_merge_high: float = 1000.0
```

在 `load_data()` 里 `_load_recipes()` 之后加 `_load_slam_config()`：

```gdscript
func load_data() -> void:
	_load_items()
	_load_item_physics_profiles()
	_load_operations()
	_load_combines()
	_load_recipes()
	_load_slam_config()
	print("[Craft] 加载 ", items.size(), " 种物品, ", _ops.size(), " 个加工节点, ", _combine.size(), " 条组合规则, ", recipes.size(), " 条容器配方")
```

在 `is_recipe_unlocked` 附近新增方法：

```gdscript
func _load_slam_config() -> void:
	var file = FileAccess.open("res://data/slam.json", FileAccess.READ)
	if file == null:
		push_warning("[Craft] slam.json 未找到，用默认力度阈值")
		return
	var data = JSON.parse_string(file.get_as_text())
	file.close()
	if data == null or not data is Dictionary:
		push_error("[Craft] slam.json 格式无效")
		return
	slam_merge_low = float(data.get("merge_low", slam_merge_low))
	slam_merge_high = float(data.get("merge_high", slam_merge_high))

func is_slam_unlocked(container: String) -> bool:
	return unlocked_slam_containers.has(container)

func unlock_slam(container: String) -> void:
	if not unlocked_slam_containers.has(container):
		unlocked_slam_containers.append(container)

## 力度分档：none=太轻不合成 / normal=窗口内 / poor=砸太狠降级
func classify_slam_force(speed: float) -> String:
	if speed < slam_merge_low:
		return "none"
	if speed > slam_merge_high:
		return "poor"
	return "normal"
```

- [ ] **Step 5：跑测试确认通过**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: `[TEST-IMPACT] ALL PASS (11 checks)`。

- [ ] **Step 6：提交**

```bash
git add scripts/systems/craft_system.gd data/slam.json scripts/test/test_impact_magic.gd
git commit -m "feat: CraftSystem 砸解锁状态 + 力度窗口分档 + slam.json"
```

---

## Task 3：CraftSystem 配方反查 `find_slam_recipe`

**Files:**
- Modify: `scripts/systems/craft_system.gd`
- Test: `scripts/test/test_impact_magic.gd`

砸合成查容器配方表，只认已解锁的 container 类；双材料对撞匹配双料配方，撞两个相同材料匹配单料配方（产双份）；`requires_purchase` 配方仍需另解锁。

- [ ] **Step 1：加失败断言**

`_ready()` 里加 `_test_find_slam_recipe()`，新增方法：

```gdscript
func _test_find_slam_recipe() -> void:
	var c = _gm().craft
	c.unlocked_slam_containers.clear()
	c.unlocked_recipes.clear()

	# 未解锁任何砸 → 无匹配
	_ok(c.find_slam_recipe(["herb", "ale"]).is_empty(), "未解锁 pot 时草药清汤不可砸")

	c.unlock_slam("pot")
	# 双料 pot 配方：herb_broth = herb + ale（不需购买）
	var r1 = c.find_slam_recipe(["herb", "ale"])
	_ok(r1.get("product", "") == "herb_broth", "草药清汤可砸")
	_ok(r1.get("double", true) == false, "双料配方 double=false")
	# 顺序无关
	_ok(c.find_slam_recipe(["ale", "herb"]).get("product", "") == "herb_broth", "食材顺序无关")

	# requires_purchase 的 pot 配方 meat_stew（生肉+麦芽）：未解锁配方 → 不可砸
	_ok(c.find_slam_recipe(["meat_raw", "ale"]).is_empty(), "肉汤配方未购则不可砸")
	c.unlock_recipe("meat_stew")
	_ok(c.find_slam_recipe(["meat_raw", "ale"]).get("product", "") == "meat_stew", "购配方后肉汤可砸")

	# 单料 barrel 配方 ale_beer = ale：需解锁 barrel + 撞两个相同
	_ok(c.find_slam_recipe(["ale", "ale"]).is_empty(), "未解锁 barrel 时麦芽酒不可砸")
	c.unlock_slam("barrel")
	var r2 = c.find_slam_recipe(["ale", "ale"])
	_ok(r2.get("product", "") == "ale_beer", "麦芽酒可砸（同料）")
	_ok(r2.get("double", false) == true, "单料配方 double=true")
	# 两个不同材料但无双料配方 → 无匹配
	_ok(c.find_slam_recipe(["ale", "grape"]).is_empty(), "无对应双料配方应空")
```

- [ ] **Step 2：跑测试确认失败**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: FAIL/ERROR（`find_slam_recipe` 不存在）。

- [ ] **Step 3：实现 `find_slam_recipe`**

在 `craft_system.gd` 末尾新增：

```gdscript
## 砸合成配方反查。keys=两个材料 key（可相同）。
## 返回 {product, container, double} 或 {}。
## double=true 表示单料配方撞两个同料 → 产双份。
## 仅匹配已 is_slam_unlocked 的 container；requires_purchase 配方需已解锁。
func find_slam_recipe(keys: Array) -> Dictionary:
	if keys.size() != 2:
		return {}
	var sorted_keys: Array = keys.duplicate()
	sorted_keys.sort()
	for product_key in recipes:
		var recipe: Dictionary = recipes[product_key]
		var container: String = recipe.get("container", "")
		if not is_slam_unlocked(container):
			continue
		if bool(recipe.get("requires_purchase", false)) and not is_recipe_unlocked(product_key):
			continue
		var ingr: Array = (recipe.get("ingredients", []) as Array).duplicate()
		ingr.sort()
		if ingr.size() == 2 and ingr == sorted_keys:
			return {"product": product_key, "container": container, "double": false}
		if ingr.size() == 1 and keys[0] == keys[1] and ingr[0] == keys[0]:
			return {"product": product_key, "container": container, "double": true}
	return {}
```

- [ ] **Step 4：跑测试确认通过**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: `[TEST-IMPACT] ALL PASS (22 checks)`。

- [ ] **Step 5：提交**

```bash
git add scripts/systems/craft_system.gd scripts/test/test_impact_magic.gd
git commit -m "feat: find_slam_recipe 容器配方反查（双料/单料/双门禁）"
```

---

## Task 4：EconomySystem 品质收益纯函数

**Files:**
- Modify: `scripts/systems/economy_system.gd`
- Test: `scripts/test/test_impact_magic.gd`

- [ ] **Step 1：加失败断言**

`_ready()` 里加 `_test_quality_payoff()`，新增方法：

```gdscript
func _test_quality_payoff() -> void:
	var e = _gm().economy
	_ok(e.gold_for_quality(10, "good") == 15, "good 金币 ×1.5")
	_ok(e.gold_for_quality(10, "normal") == 10, "normal 金币 ×1")
	_ok(e.gold_for_quality(10, "poor") == 5, "poor 金币 ×0.5")
	_ok(e.gold_for_quality(7, "poor") == 3, "poor 金币向下取整 floor(3.5)=3")
	_ok(e.gold_for_quality(10, "") == 10, "未知品质回退 ×1")
	_ok(e.reputation_for_quality("good") == 3, "good 声望 +3")
	_ok(e.reputation_for_quality("normal") == 2, "normal 声望 +2")
	_ok(e.reputation_for_quality("poor") == 0, "poor 声望 +0")
	_ok(e.reputation_for_quality("") == 2, "未知品质声望回退 +2")
```

- [ ] **Step 2：跑测试确认失败**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: FAIL/ERROR（`gold_for_quality` 不存在）。

- [ ] **Step 3：实现纯函数**

在 `economy_system.gd` 顶部（`var gold` 等字段附近）新增常量：

```gdscript
const QUALITY_GOLD_MULT := {"good": 1.5, "normal": 1.0, "poor": 0.5}
const QUALITY_REP := {"good": 3, "normal": 2, "poor": 0}
```

在文件末尾新增：

```gdscript
## 按成品品质算上菜金币：good ×1.5 / normal ×1 / poor ×0.5，未知回退 ×1。
func gold_for_quality(price: int, quality: String) -> int:
	var mult: float = float(QUALITY_GOLD_MULT.get(quality, 1.0))
	return floori(price * mult)

## 按成品品质算声望增量：good +3 / normal +2 / poor +0，未知回退 +2。
func reputation_for_quality(quality: String) -> int:
	return int(QUALITY_REP.get(quality, 2))
```

- [ ] **Step 4：跑测试确认通过**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: `[TEST-IMPACT] ALL PASS (31 checks)`。

- [ ] **Step 5：提交**

```bash
git add scripts/systems/economy_system.gd scripts/test/test_impact_magic.gd
git commit -m "feat: EconomySystem 品质→收益纯函数（gold/reputation）"
```

---

## Task 5：GameManager 接入 buy_ability + 上菜消费 quality

**Files:**
- Modify: `scripts/game_manager.gd`
- Test: `scripts/test/test_impact_magic.gd`

- [ ] **Step 1：加失败断言**

`_ready()` 里加 `_test_buy_and_serve()`，新增方法：

```gdscript
func _test_buy_and_serve() -> void:
	var gm = _gm()
	gm.craft.unlocked_slam_containers.clear()

	# 钱不够买不了
	gm.economy.gold = 0
	_ok(not gm.buy_ability("slam_pot"), "无钱不能购买")
	_ok(not gm.craft.is_slam_unlocked("pot"), "购买失败不解锁")

	# 钱够：扣钱 + 解锁
	gm.economy.gold = 200
	_ok(gm.buy_ability("slam_pot"), "有钱购买成功")
	_ok(gm.economy.gold == 140, "购买后扣 60 金")
	_ok(gm.craft.is_slam_unlocked("pot"), "购买后 pot 解锁")
	_ok(gm.is_ability_owned("slam_pot"), "is_ability_owned 反映已购")

	# 重复购买被拒、不二次扣钱
	_ok(not gm.buy_ability("slam_pot"), "重复购买被拒")
	_ok(gm.economy.gold == 140, "重复购买不扣钱")

	# 未知能力 key 被拒
	_ok(not gm.buy_ability("nope"), "未知能力购买被拒")
```

- [ ] **Step 2：跑测试确认失败**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: FAIL/ERROR（`buy_ability` 不存在）。

- [ ] **Step 3：实现 buy_ability / is_ability_owned**

在 `game_manager.gd` 的 `buy_recipe_unlock` 之后新增（含能力→容器映射常量，放在文件顶部 `var shop` 附近的常量区或方法上方）：

```gdscript
const ABILITY_TO_CONTAINER := {"slam_pot": "pot", "slam_barrel": "barrel"}

func buy_ability(key: String) -> bool:
	var container: String = ABILITY_TO_CONTAINER.get(key, "")
	if container == "":
		return false
	if craft.is_slam_unlocked(container):
		return false
	var price: int = shop.get_ability_price(key)
	if price <= 0:
		return false
	if not economy.spend_gold(price):
		return false
	craft.unlock_slam(container)
	return true

func is_ability_owned(key: String) -> bool:
	var container: String = ABILITY_TO_CONTAINER.get(key, "")
	return container != "" and craft.is_slam_unlocked(container)
```

- [ ] **Step 4：上菜消费 quality**

在 `_on_serve_requested` 的成功分支，把：

```gdscript
	if success:
		economy.add_gold(item_price)
		economy.add_reputation(2)
```

改为：

```gdscript
	if success:
		var serve_quality: String = String(craft_style_data.get("quality", "normal"))
		economy.add_gold(economy.gold_for_quality(item_price, serve_quality))
		economy.add_reputation(economy.reputation_for_quality(serve_quality))
```

（`craft_style_data` 已携带 `quality` 键：`bar_workspace._serve_formal` 传 `{"serve_drop_speed": speed, "quality": item.quality}`。）

- [ ] **Step 5：跑测试确认通过**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: `[TEST-IMPACT] ALL PASS (40 checks)`。

- [ ] **Step 6：提交**

```bash
git add scripts/game_manager.gd scripts/test/test_impact_magic.gd
git commit -m "feat: GameManager buy_ability + 上菜按品质结算收益"
```

---

## Task 6：存档持久化已购能力

**Files:**
- Modify: `scripts/game_manager.gd`
- Test: `scripts/test/test_impact_magic.gd`

- [ ] **Step 1：加失败断言**

`_ready()` 里加 `_test_save_roundtrip()`，新增方法：

```gdscript
func _test_save_roundtrip() -> void:
	var gm = _gm()
	gm.craft.unlocked_slam_containers.clear()
	gm.craft.unlock_slam("barrel")
	var snap: Dictionary = gm._capture_save_state()
	# 改脏后再恢复，验证从快照读回
	gm.craft.unlocked_slam_containers.clear()
	gm._apply_save_state(snap)
	_ok(gm.craft.is_slam_unlocked("barrel"), "存档往返保住已购能力")
	_ok(not gm.craft.is_slam_unlocked("pot"), "未购的不应被恢复出来")
```

> 注：`_capture_save_state` 是现有快照方法名。若实际私有方法名不同（见 `game_manager.gd` 中 `"craft": {"unlocked_recipes": ...}` 所在函数），用该函数实名替换断言里的 `_capture_save_state`。

- [ ] **Step 2：跑测试确认失败**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: FAIL（`is_slam_unlocked("barrel")` 为 false，快照没存该字段）。

- [ ] **Step 3：存档写入**

在快照构建处把：

```gdscript
		"craft": {"unlocked_recipes": craft.unlocked_recipes.duplicate()},
```

改为：

```gdscript
		"craft": {
			"unlocked_recipes": craft.unlocked_recipes.duplicate(),
			"unlocked_slam_containers": craft.unlocked_slam_containers.duplicate(),
		},
```

- [ ] **Step 4：存档读回**

在 `_apply_save_state` 里 `unlocked_recipes` 恢复之后插入：

```gdscript
	craft.unlocked_slam_containers.clear()
	for sc in data.get("craft", {}).get("unlocked_slam_containers", []):
		craft.unlocked_slam_containers.append(String(sc))
```

同时在 `_default_new_game_state`（构造默认存档处，含 `"craft": {"unlocked_recipes": []}`）把该项补成：

```gdscript
		"craft": {"unlocked_recipes": [], "unlocked_slam_containers": []},
```

- [ ] **Step 5：跑测试确认通过**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
```
Expected: `[TEST-IMPACT] ALL PASS (42 checks)`。

- [ ] **Step 6：回归既有存档测试**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_save_roundtrip.tscn"
```
Expected: 既有存档往返测试仍 PASS（无 FAIL/ERROR）。

- [ ] **Step 7：提交**

```bash
git add scripts/game_manager.gd scripts/test/test_impact_magic.gd
git commit -m "feat: 存档持久化已购冲击魔法能力"
```

---

## Task 7：bar_workspace 物理碰撞 → 砸合成

**Files:**
- Modify: `scripts/ui/bar_workspace.gd`
- 参考移植源：`scripts/ui/experimental_tavern_view.gd:451-566`（碰撞连接 + merge）

本任务是物理胶水，逻辑判定全部委托 Task 2/3 的纯函数，编辑器走查验证（Task 8）。**不重写厨具**——砸合成是新增旁路，只对"两个材料 DeskItem"高速对撞生效，产物/容器碰撞不触发。

- [ ] **Step 1：碰撞连接**

`bar_workspace.gd` 的 `_on_items_child_added`（现连 `fell_out_of_bounds`）里追加 `body_entered` 连接：

```gdscript
func _on_items_child_added(child: Node) -> void:
	if child is DeskItem:
		if not child.fell_out_of_bounds.is_connected(_on_desk_item_fell):
			child.fell_out_of_bounds.connect(_on_desk_item_fell)
		if not child.body_entered.is_connected(_on_item_collision.bind(child)):
			child.body_entered.connect(_on_item_collision.bind(child))
```

> 前提已满足：`DeskItem._ready()`（`scripts/test/desk_item.gd:69-71`）已设 `contact_monitor = true`、`max_contacts_reported = 4`，并自连了内部 `_on_body_entered`。本步追加的 `_on_item_collision` 是该信号的**第二个**监听者（信号支持多连接，互不干扰），**无需改 `.tscn`**。

- [ ] **Step 2：碰撞处理（移植 + 改判定）**

在 `bar_workspace.gd` 末尾新增。相对速度用 CraftSystem 分档；配方用 `find_slam_recipe`；只处理两个材料 DeskItem：

```gdscript
## 两个材料 DeskItem 高速对撞 → 按力度窗口砸合成。
## a=信号发出者，b=被撞对象。产物/容器/低速碰撞不触发。
func _on_item_collision(b: Node, a: DeskItem) -> void:
	if not (b is DeskItem):
		return
	var other: DeskItem = b as DeskItem
	if a.is_queued_for_deletion() or other.is_queued_for_deletion():
		return
	var key_a: String = a.item_key
	var key_b: String = other.item_key
	if key_a == "" or key_b == "":
		return
	# 仅材料参与砸合成（产物不可再砸）
	if _gm.craft.is_product(key_a) or _gm.craft.is_product(key_b):
		return
	var rel_speed: float = (a.linear_velocity - other.linear_velocity).length()
	var force_tier: String = _gm.craft.classify_slam_force(rel_speed)
	if force_tier == "none":
		return
	var recipe: Dictionary = _gm.craft.find_slam_recipe([key_a, key_b])
	if recipe.is_empty():
		return
	var center: Vector2 = (a.global_position + other.global_position) * 0.5
	var conserved: Vector2 = (a.linear_velocity + other.linear_velocity) * 0.5
	call_deferred("_do_slam_merge", a, other, recipe, force_tier, center, conserved)


func _do_slam_merge(a: DeskItem, other: DeskItem, recipe: Dictionary, force_tier: String, center: Vector2, conserved: Vector2) -> void:
	if not is_instance_valid(a) or not is_instance_valid(other):
		return
	if a.is_queued_for_deletion() or other.is_queued_for_deletion():
		return
	a.queue_free()
	other.queue_free()
	var quality: String = "poor" if force_tier == "poor" else "normal"
	var product_key: String = recipe["product"]
	if bool(recipe.get("double", false)):
		var p1 := _spawn_slam_product(center + Vector2(-18, 0), product_key, quality)
		p1.linear_velocity = conserved + Vector2(0, -40)
		var p2 := _spawn_slam_product(center + Vector2(18, 0), product_key, quality)
		p2.linear_velocity = conserved + Vector2(0, 40)
	else:
		var p := _spawn_slam_product(center, product_key, quality)
		p.linear_velocity = conserved
	_gm.play_audio_event("product_ready")
	if quality == "poor":
		_gm.notify_stage_caption("手重了，砸出了次品", ThemeColors.TEXT_SUBTITLE)


func _spawn_slam_product(pos: Vector2, product_key: String, quality: String) -> DeskItem:
	var item := _spawn_desk_item_at(pos, product_key)
	item.quality = quality
	return item
```

> 复用现有 `_spawn_desk_item_at`（已 add_child + set_item + 连 readable）。`_on_items_child_added` 会在 add_child 时自动给新产物连上 `body_entered`/`fell_out_of_bounds`——但产物因 `is_product` 守卫不会再砸。

- [ ] **Step 3：GameManager 转发舞台浮字**

`bar_workspace` 只跟 GameManager 说话（mediator 规则），不直接引用 `tavern_view`。在 `game_manager.gd` 新增转发（放 `_refresh_tavern_ui` 附近）：

```gdscript
func notify_stage_caption(text: String, color: Color = Color.WHITE) -> void:
	if _tavern_view != null and is_instance_valid(_tavern_view):
		_tavern_view.show_stage_caption(text, color)
```

- [ ] **Step 4：编译检查（无单测，物理走 Task 8）**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" --editor --quit
```
Expected: 无 `SCRIPT ERROR` / `Parse Error`（看 `bar_workspace.gd` / `game_manager.gd` 相关报错）。

- [ ] **Step 5：跑既有 Tavern 相关回归 + 本功能单测**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_kitchen_containers.tscn"
```
Expected: 两者皆 `ALL PASS`（确认未碰坏厨具逻辑）。

- [ ] **Step 6：提交**

```bash
git add scripts/ui/bar_workspace.gd scripts/game_manager.gd
git commit -m "feat: 吧台两材料对撞砸合成（力度窗口→品质）+ 舞台浮字转发"
```

---

## Task 8：商店"技法"区 UI

**Files:**
- Modify: `scripts/ui/day_map_view.gd`
- 参考：同文件 `_build_recipe_rows`（行模板）、`_build_shop_ui`（区块挂载）

- [ ] **Step 1：在商店内容区加"技法"标题与列表容器**

在 `_build_shop_ui()` 里、配方区（`_recipe_list` 挂载）之后追加。先看现有 `_recipe_title` / `_recipe_list` 的建法照搬：

```gdscript
	var ability_title = Label.new()
	ability_title.text = "技法"
	ThemeColors.style_header(ability_title, 18)
	shop_content.add_child(ability_title)

	_ability_list = VBoxContainer.new()
	_ability_list.add_theme_constant_override("separation", 6)
	shop_content.add_child(_ability_list)
```

并在文件顶部声明字段（与 `_recipe_list` 同处）：

```gdscript
var _ability_list: VBoxContainer
```

- [ ] **Step 2：在 `_refresh_shop_ui()` 里调用构建**

`_refresh_shop_ui()` 末尾、`_update_gold_display()` 之前加 `_build_ability_rows(gm)`。

- [ ] **Step 3：实现 `_build_ability_rows`**

照 `_build_recipe_rows` 风格，已购显示"已掌握"灰态：

```gdscript
func _build_ability_rows(gm) -> void:
	for child in _ability_list.get_children():
		child.queue_free()

	for key in gm.shop.get_ability_keys():
		var row = HBoxContainer.new()
		row.add_theme_constant_override("separation", 8)
		row.custom_minimum_size = Vector2(0, 40)

		var name_label = Label.new()
		name_label.text = gm.shop.get_ability_name(key)
		name_label.custom_minimum_size = Vector2(150, 0)
		name_label.add_theme_color_override("font_color", ThemeColors.TEXT_LIGHT)
		name_label.add_theme_font_size_override("font_size", 16)
		row.add_child(name_label)

		var owned: bool = gm.is_ability_owned(key)
		if owned:
			var owned_label = Label.new()
			owned_label.text = "已掌握"
			owned_label.add_theme_color_override("font_color", ThemeColors.TEXT_SUBTITLE)
			owned_label.add_theme_font_size_override("font_size", 14)
			row.add_child(owned_label)
		else:
			var price_label = Label.new()
			price_label.text = str(gm.shop.get_ability_price(key)) + "金"
			price_label.custom_minimum_size = Vector2(70, 0)
			price_label.add_theme_color_override("font_color", ThemeColors.AMBER_PRIMARY)
			price_label.add_theme_font_size_override("font_size", 14)
			row.add_child(price_label)

			var buy_btn = Button.new()
			buy_btn.text = "购买"
			buy_btn.custom_minimum_size = Vector2(56, 30)
			ThemeColors.style_button(buy_btn, 14)
			buy_btn.pressed.connect(func():
				if gm.buy_ability(key):
					_refresh_shop_ui()
			)
			row.add_child(buy_btn)

		_ability_list.add_child(row)
```

- [ ] **Step 4：编译检查**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" --editor --quit
```
Expected: 无 `SCRIPT ERROR` / `Parse Error`。

- [ ] **Step 5：提交**

```bash
git add scripts/ui/day_map_view.gd
git commit -m "feat: 商店技法区——购买冲击魔法能力（已掌握态）"
```

---

## Task 9：编辑器全链路走查 + 总回归

**Files:** 无（验证任务）

- [ ] **Step 1：全量编译检查**

Run:
```
<godot> --headless --path "D:/game/tavern-manager" --editor --quit
```
Expected: 0 `SCRIPT ERROR` / 0 `Parse Error`。

- [ ] **Step 2：跑全部相关 headless 测试**

Run（逐个）:
```
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_impact_magic.tscn"
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_kitchen_containers.tscn"
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_save_roundtrip.tscn"
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_l3.tscn"
<godot> --headless --path "D:/game/tavern-manager" "res://scenes/test/test_barrel_shake.tscn"
```
Expected: 每个 `ALL PASS`，无 FAIL/ERROR。（`test_l3` / `test_barrel_shake` 确认 quality 改动未破坏 L3 与酒桶。）

- [ ] **Step 3：编辑器 GUI 走查清单（逻辑测试覆盖不到的）**

在 Godot 编辑器打开项目（确认开的是主 checkout，见 [[godot-worktree-inside-project-gotcha]]），运行主场景走核心路径：

- [ ] DayMap → 商店 → 技法区显示两档魔法、价格正确
- [ ] 钱不够时购买无反应；钱够购买后该行变"已掌握"、金币扣减
- [ ] 进 Tavern：取两个对应材料（如生肉+麦芽，且已购肉汤配方），**轻轻**碰一起 → 不合成
- [ ] 用力甩到一起（窗口内）→ 出肉汤，上菜得**全额**金币
- [ ] 狠狠砸（超上限）→ 出肉汤但舞台浮字"手重了"，上菜金币**减半**、不加声望
- [ ] 未购买酒桶魔法时，砸两个麦芽 → 不出麦芽酒
- [ ] 已购酒桶魔法 → 砸两个麦芽 → 出 **2 份**麦芽酒
- [ ] 厨具路径（炖锅投料搅拌 / 酒桶摇）仍照常工作，互不干扰
- [ ] **存档验证**：购能力 → 进 LedgerScreen 触发存档 → 回标题"继续游戏" → 商店技法区仍"已掌握"
- [ ] 全程 0 error / 0 warning

- [ ] **Step 4：更新交接文档（可选但推荐）**

若按 `docs/` 惯例需要交接文档，新建 `docs/NN_冲击魔法系统.md` 记录：力度阈值在 `data/slam.json`、价格在 `data/shop.json`、品质倍率在 `economy_system.gd` 常量；走查清单同上。提交：

```bash
git add docs/NN_冲击魔法系统.md
git commit -m "docs: 冲击魔法系统交接（调参位置 + 走查清单）"
```

---

## Self-Review 记录

- **Spec 覆盖**：①商店两档能力→Task 1/8 ②力度窗口→Task 2 ③配方反查双门禁→Task 3 ④quality 收益→Task 4/5 ⑤存档→Task 6 ⑥物理砸合成进 Tavern→Task 7 ⑦烤架不做/不重写厨具→Task 7 守卫 + 不动 grill ⑧已知影响（酒桶经济）→Task 9 Step 2 用 test_barrel_shake 兜底。无遗漏。
- **类型一致**：`unlocked_slam_containers`（容器字符串数组）、`is_slam_unlocked(container)`、`unlock_slam(container)`、`classify_slam_force(speed)->"none"/"normal"/"poor"`、`find_slam_recipe(keys)->{product,container,double}`、`gold_for_quality`/`reputation_for_quality`、`buy_ability`/`is_ability_owned`、`notify_stage_caption` 全计划一致。
- **占位符**：无 TODO/TBD；每个代码步给出完整代码。Task 6 Step 1 与 Task 7 Step 1 各留一处"按实际方法名/.tscn 现状核对"的明确说明，不是占位符而是核对指引。
