# Bug 修复：EndNight 流程 & 调试日志清理

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 EndNight 跳过 LedgerScreen 的流程 bug，清理开发期调试日志和死代码。

**Architecture:** 四个独立修复任务，互不依赖。按顺序执行（先修流程 bug，再做清理）。

**Tech Stack:** Godot 4.6 .NET / C# — `GameManager.cs`, `CraftStation.cs`, `DayCycleSystem.cs`, `LedgerScreen.cs`

---

### Task 1: 修复 EndNight 跳过 LedgerScreen

**Files:**
- Modify: `scripts/GameManager.cs:445-446`

- [ ] **Step 1: 将 EndNight 的收尾逻辑改回导航到 LedgerScreen**

`EndNight()` 末尾当前代码（Line ~445-446）：

```csharp
        GD.Print("[GameManager] 打烊完成，进入下一天");
        DayCycle.NextPhase();
```

替换为：

```csharp
        GD.Print("[GameManager] 切换到 LedgerScreen");
        GetTree().CallDeferred("change_scene_to_file", "res://scenes/ui/LedgerScreen.tscn");
```

逻辑说明：`EndNight()` 构造 `CurrentLedgerData` 后应导航到 `LedgerScreen` 展示结算。`LedgerScreen.OnContinue()` 按钮点击后调用 `DayCycle.NextPhase()`，触发 `OnPhaseChanged(Day)` 进入下一天的 DayMap。直接调用 `NextPhase` 会跳过结算界面。

- [ ] **Step 2: 构建验证**

```bash
cd tavern-manager && dotnet build
```
预期：0 errors, 0 warnings

- [ ] **Step 3: Commit**

```bash
git add scripts/GameManager.cs
git commit -m "fix: restore LedgerScreen navigation in EndNight"
```

---

### Task 2: 清理 CraftStation 调试日志

**Files:**
- Modify: `scripts/ui/CraftStation.cs:234-240, 249, 322-331, 335, 346`

- [ ] **Step 1: 移除 `_inputFrameCounter` 及周期日志**

删除以下代码块（Line 234, 238-240）：

```csharp
    private int _inputFrameCounter;

    public override void _Input(InputEvent e)
    {
        _inputFrameCounter++;
        if (_inputFrameCounter % 120 == 1)
            GD.Print($"[CraftStation] _Input alive (frame {Engine.GetProcessFrames()}), mouseFilter={MouseFilter}, visible={Visible}");

        if (_overlayMenu?.Visible == true) return;
```

替换为：

```csharp
    public override void _Input(InputEvent e)
    {
        if (_overlayMenu?.Visible == true) return;
```

- [ ] **Step 2: 移除拖拽释放日志**

Line 249 — 将内联日志提取：

```csharp
                else if (!mb.Pressed && _dragging) { GD.Print($"[CraftStation] _Input DRAG RELEASE at pos=({mb.Position.X:F0},{mb.Position.Y:F0}), mat={_dragMaterial}"); TryDrop(mb.Position); }
```

替换为：

```csharp
                else if (!mb.Pressed && _dragging) TryDrop(mb.Position);
```

- [ ] **Step 3: 移除 `TryDrop` 内的调试日志**

`TryDrop` 方法开头的日志（Line 322）：

```csharp
        GD.Print($"[CraftStation] TryDrop at pos=({pos.X:F0},{pos.Y:F0}), mat={_dragMaterial}");
```

删除该行。

CustomerArea hit test 日志（Line 330-331）：

```csharp
            var caRect = customerArea.GetGlobalRect();
            GD.Print($"[CraftStation] CustomerArea rect=({caRect.Position.X:F0},{caRect.Position.Y:F0})-({caRect.End.X:F0},{caRect.End.Y:F0}), hit={HitTest(customerArea, pos)}");
```

替换为：

```csharp
            var caRect = customerArea.GetGlobalRect();
```

但 `caRect` 变量在移除日志后未被使用（`HitTest` 只用了 `customerArea` 和 `pos`），所以整行删除：

```csharp
            if (HitTest(customerArea, pos))
```

Line 335 — 删除 `TryDrop→CustomerArea` 日志行：

```csharp
                GD.Print($"[CraftStation] TryDrop→CustomerArea, mat={_dragMaterial}, hasGuest={gm.Guests.HasGuest}, isRecipe={_dragMaterial != null && gm.Craft.Recipes.ContainsKey(_dragMaterial)}");
```

Line 346 — 删除 `TryDrop→ServeRequested` 日志行：

```csharp
                    GD.Print($"[CraftStation] TryDrop→ServeRequested, CraftedKey={gm.Craft.CraftedKey}");
```

- [ ] **Step 4: 构建验证**

```bash
cd tavern-manager && dotnet build
```
预期：0 errors, 0 warnings

- [ ] **Step 5: Commit**

```bash
git add scripts/ui/CraftStation.cs
git commit -m "fix: remove debug logging from CraftStation"
```

---

### Task 3: 移除 GameManager 调试日志 & 死代码

**Files:**
- Modify: `scripts/GameManager.cs:42-43, 111, 117, 166, 171, 178, 186-187, 202-204, 213, 334`

- [ ] **Step 1: 移除 `MN` 死代码字典（Line 42-43）**

```csharp
    private static readonly Dictionary<string, string> MN = new()
    { ["Ale"] = "麦芽", ["Wine"] = "葡萄", ["Bread"] = "面粉", ["Meat"] = "生肉", ["Herb"] = "草药", ["SleepPowder"] = "沉睡花粉" };
```

删除这两行（该字典从未被引用，且与 `recipes.json` 中的名称重复）。

- [ ] **Step 2: 清理 RegisterView 内的调试日志**

Line 111:
```csharp
            GD.Print("[GameManager] RegisterView: TavernView detected");
```
删除。

Line 117:
```csharp
            GD.Print($"[GameManager] RegisterView: CraftStation={(craftStation != null ? "found" : "NULL")}");
```
删除。

- [ ] **Step 3: 清理 ServeRequested 内的调试日志**

Line 166:
```csharp
                    GD.Print($"[GameManager] ServeRequested — HasGuest={Guests.HasGuest}, CraftedKey={Craft.CraftedKey}");
```
删除。

Line 171:
```csharp
                        GD.Print($"[GameManager] ServeRequested — 手势未完成: {Craft.CraftedKey}");
```
删除。

Line 178:
```csharp
                    GD.Print($"[GameManager] ServeRequested — isImportant={isImportant}, npcId={npcId}, OrderKey={Guests.CurrentGuest.OrderKey}");
```
删除。

Line 186-187:
```csharp
                        if (isImportant) Narrative.SetVar("serve_result", "success");
                        GD.Print($"[GameManager] ServeRequested — 成功，serve_result=success");
```
保留 `SetVar` 行，删除后面的 `GD.Print` 行。

Line 193-194:
```csharp
                        if (isImportant) Narrative.SetVar("serve_result", "fail");
                        GD.Print($"[GameManager] ServeRequested — 失败，serve_result=fail");
```
保留 `SetVar` 行，删除后面的 `GD.Print` 行。

- [ ] **Step 4: 清理 post 对话路径日志**

Line 202-204 区域，保留 `var exists = ResourceLoader.Exists(postPath);`，删除前后的两行 `GD.Print`:

删除:
```csharp
                        var exists = ResourceLoader.Exists(postPath);
                        GD.Print($"[GameManager] ServeRequested — postPath={postPath}, exists={exists}");
```
保留 `var exists = ...`，删除 `GD.Print`。

Line 213:
```csharp
                            GD.Print($"[GameManager] post 对话不存在: {postPath}，直接清除客人");
```
删除。

- [ ] **Step 5: 清理 OnGuestArrived 调试日志**

Line 334:
```csharp
            GD.Print($"[GameManager] OnGuestArrived — IMPORTANT NPC: {guest.NpcId}, loading pre: {dialoguePath}");
```
删除。

- [ ] **Step 6: 构建验证**

```bash
cd tavern-manager && dotnet build
```
预期：0 errors, 0 warnings

- [ ] **Step 7: Commit**

```bash
git add scripts/GameManager.cs
git commit -m "fix: remove debug logging and dead MN dictionary from GameManager"
```

---

### Task 4: 清理 DayCycleSystem & LedgerScreen 调试日志

**Files:**
- Modify: `scripts/systems/DayCycleSystem.cs:31, 34, 42`
- Modify: `scripts/ui/LedgerScreen.cs:79, 81, 83`

- [ ] **Step 1: 移除 DayCycleSystem.NextPhase 内的调试日志**

Line 31:
```csharp
        GD.Print($"[DayCycle] NextPhase called, current Phase={Phase}");
```
删除。

Line 34-35 (Day→Night 分支):
```csharp
            Phase = DayPhase.Night;
            GD.Print("[DayCycle] Day → Night, invoking PhaseChanged");
            PhaseChanged?.Invoke(Phase);
```
保留 `Phase = DayPhase.Night;` 和 `PhaseChanged?.Invoke(Phase);`，删除中间 `GD.Print`。

Line 42-43 (Night→Day 分支):
```csharp
            Phase = DayPhase.Day;
            StartDay();
            GD.Print("[DayCycle] Night → Day, invoking PhaseChanged");
            PhaseChanged?.Invoke(Phase);
```
删除 `GD.Print` 行，保留其余。

- [ ] **Step 2: 移除 LedgerScreen.OnContinue 内的调试日志**

Line 79, 81, 83:
```csharp
        GD.Print("[LedgerScreen] 继续按钮点击，调用 NextPhase");
        var gm = GetNode<GameManager>("/root/GameManager");
        GD.Print($"[LedgerScreen] 当前 Phase={gm.DayCycle.Phase}, CurrentDay={gm.Economy.CurrentDay}");
        gm.DayCycle.NextPhase();
        GD.Print($"[LedgerScreen] NextPhase 完成, Phase={gm.DayCycle.Phase}, CurrentDay={gm.Economy.CurrentDay}");
```

替换为：

```csharp
        var gm = GetNode<GameManager>("/root/GameManager");
        gm.DayCycle.NextPhase();
```

- [ ] **Step 3: 构建验证**

```bash
cd tavern-manager && dotnet build
```
预期：0 errors, 0 warnings

- [ ] **Step 4: Commit**

```bash
git add scripts/systems/DayCycleSystem.cs scripts/ui/LedgerScreen.cs
git commit -m "fix: remove debug logging from DayCycleSystem and LedgerScreen"
```
